{
  "name": "aolie_monitor_front",
  "version": "0.0.4",
  "description": "监控显示组件，做首页模块",
  "main": "index.js",
  "author": "xxl",
  "license": "ISC",
  "scripts": {
    "build": "webpack --progress --colors --bail --config ./webpack.product.config.js",
    "dev": "webpack --progress --colors --open --config ./webpack.dev.config.js",
    "start": "webpack-dev-server  --devtool eval --open  --config ./webpack.dev.config.js"
  },
  "config": {
    "buildDir": "./build",
    "devHost": "localhost",
    "devPort": 5000,
    "mockHost": "localhost",
    "mockPort": 6000,
    "publicPath": "luckyCloud"
  },
  "dependencies": {
    "aolie_core": "^0.0.7"
  },
  "devDependencies": {
    "webpack-cli": "^3.3.12"
  }
}
